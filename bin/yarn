#!/usr/bin/env bash
set -Eeuo pipefail
DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
source "$DIR/common.sh"

tool="$(basename "$0")"
if [[ "$tool" == "yarn" || "$tool" == "pnpm" ]]; then
  docker_exec bash -lc "corepack enable >/dev/null 2>&1 || true; ${tool} \"\$@\"" -- "$@"
else
  docker_exec "${tool}" "$@"
fi
